# Port to run the server on
PORT=5000

# Secret key for Flask session security
# Generate a strong random key in production
SECRET_KEY=your_secret_key_here

# Time in seconds after which inactive sessions are cleaned up
INACTIVE_TIMEOUT=3600

# Base directory for user home directories
HOME_DIR=/app/storage/home
